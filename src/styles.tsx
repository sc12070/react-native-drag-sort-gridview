import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  list: {
    width: '100%',
    overflow: 'visible'
  },
  content: {
    flexDirection: 'row',
    flexWrap: 'wrap'
  },
  uiBlock: {
    position: 'absolute',
    top: 0,
    bottom: 0,
    left: 0,
    right: 0
  }
})

export default styles
