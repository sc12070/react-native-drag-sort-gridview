import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  list: {
    width: '100%',
    overflow: 'visible'
  },
  content: {
    flexDirection: 'row',
    flexWrap: 'wrap'
  },
  uiBlock: {
    position: 'absolute',
    width: '100%',
    height: '100%'
  }
})

export default styles
